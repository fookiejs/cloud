service: fookie-cloud
frameworkVersion: "2"
provider:
  name: aws
  runtime: nodejs14.x
  lambdaHashingVersion: 20201221
  region: eu-central-1
functions:
  run:
    handler: handler.run
    events:
      - httpApi:
          method: post
          path: /
plugins:
  - serverless-offline
