version: "3.7"
services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "80:8080"
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "2626:2626"
  mongo:
    image: mongo
